using System.Windows;

namespace BTree2018
{
    public partial class FileViewer : Window
    {
        
        
        public FileViewer(string pageFilePath, string recordFilePath, string pageMapFilePath, string recordMapFilePath)
        {
            InitializeComponent();
        }
    }
}
